<?php
/*	    if( preg_match('/^\+\d{2}\(\d{3}\)\d{3}-\d{2}-\d{2}$/', '+38(050)759-20-1' ) == 1 ){
		  echo 'Yeah';
		}else{
			echo 'No';
		}
*/
//    var_dump(scandir($_SERVER['DOCUMENT_ROOT'].'/app/'));
//  echo opendir($_SERVER['DOCUMENT_ROOT'].'/app/');
/*  $db = new PDO('mysql:host=localhost;dbname=message_form', 'root', '');
  $stmt = $db->prepare('INSERT INTO messages(first_name, email) VALUES (?, ?)');
//  $stmt->bindParam('one', 'two');
  $stmt->execute(['one','two']);
*/
/*
  $db = new PDO('mysql:host=localhost;dbname=message_form', 'root', '');
  $stmt = $db->prepare('SELECT * FROM messages');
  $stmt->execute();
  $data = $stmt->fetchAll(PDO::FETCH_ASSOC);
  var_dump($data);
*/
  echo "<script>window.location = 'http://savchenkoportfolio/aprimindtask/controllers/showController.php'</script>";
?>